import { Query as GherkinQuery } from '@cucumber/gherkin-utils';
import { Query as CucumberQuery } from '@cucumber/query';
import { messages } from '@cucumber/messages';
export default function filterByStatus(gherkinDocument: messages.IGherkinDocument, gherkinQuery: GherkinQuery, cucumberQuery: CucumberQuery, statuses: messages.TestStepFinished.TestStepResult.Status[]): messages.IGherkinDocument | null;
