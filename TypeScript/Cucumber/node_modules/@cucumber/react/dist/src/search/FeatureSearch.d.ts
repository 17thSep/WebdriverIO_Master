import { messages } from '@cucumber/messages';
export default class FeatureSearch {
    private readonly featuresByUri;
    private readonly index;
    add(gherkinDocument: messages.IGherkinDocument): void;
    search(query: string): messages.GherkinDocument.IFeature[];
}
