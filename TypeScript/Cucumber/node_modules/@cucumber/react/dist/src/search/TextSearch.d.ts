import { messages } from '@cucumber/messages';
export default class TextSearch {
    private readonly featureSearch;
    private readonly backgroundSearch;
    private readonly scenarioSearch;
    private readonly stepSearch;
    private readonly ruleSearch;
    private readonly gherkinDocuments;
    search(query: string): messages.IGherkinDocument[];
    add(gherkinDocument: messages.IGherkinDocument): void;
}
