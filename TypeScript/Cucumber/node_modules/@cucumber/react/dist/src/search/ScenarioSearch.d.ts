import { messages } from '@cucumber/messages';
export default class ScenarioSearch {
    private readonly index;
    private scenarioById;
    add(scenario: messages.GherkinDocument.Feature.IScenario): void;
    search(query: string): messages.GherkinDocument.Feature.IScenario[];
}
