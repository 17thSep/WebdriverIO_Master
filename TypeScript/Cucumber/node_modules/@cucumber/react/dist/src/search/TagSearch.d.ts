import { messages } from '@cucumber/messages';
import { Query as GherkinQuery } from '@cucumber/gherkin-utils';
export default class TagSearch {
    private readonly gherkinQuery;
    private readonly pickleById;
    private readonly picklesByScenarioId;
    private gherkinDocuments;
    constructor(gherkinQuery: GherkinQuery);
    search(query: string): messages.IGherkinDocument[];
    add(gherkinDocument: messages.IGherkinDocument): void;
}
