import { messages } from '@cucumber/messages';
export default class RuleSearch {
    private readonly index;
    private ruleById;
    add(rule: messages.GherkinDocument.Feature.FeatureChild.IRule): void;
    search(query: string): messages.GherkinDocument.Feature.FeatureChild.IRule[];
}
