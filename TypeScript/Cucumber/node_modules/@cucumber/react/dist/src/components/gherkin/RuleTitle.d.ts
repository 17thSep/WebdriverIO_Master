import React from 'react';
import { messages } from '@cucumber/messages';
interface IProps {
    id: string;
    rule: messages.GherkinDocument.Feature.FeatureChild.IRule;
}
declare const RuleTitle: React.FunctionComponent<IProps>;
export default RuleTitle;
