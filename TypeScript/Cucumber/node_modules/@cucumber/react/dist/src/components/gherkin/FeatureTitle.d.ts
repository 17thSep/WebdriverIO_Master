import React from 'react';
import { messages } from '@cucumber/messages';
interface IProps {
    id: string;
    feature: messages.GherkinDocument.IFeature;
}
declare const FeatureTitle: React.FunctionComponent<IProps>;
export default FeatureTitle;
