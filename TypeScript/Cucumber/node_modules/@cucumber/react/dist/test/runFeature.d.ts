import { SupportCode, IHook, ISupportCodeExecutor } from '@cucumber/fake-cucumber';
import { Query as GherkinQuery } from '@cucumber/gherkin-utils';
import { messages } from '@cucumber/messages';
export declare class FailingCodeSupport implements ISupportCodeExecutor {
    readonly stepDefinitionId: string;
    constructor(stepDefinitionId: string);
    execute(): void;
    argsToMessages(): messages.TestCase.TestStep.StepMatchArgumentsList.IStepMatchArgument[];
}
export declare class FailingHook implements IHook {
    readonly id: string;
    constructor(id: string);
    match(): ISupportCodeExecutor;
    toMessage(): messages.IEnvelope;
}
export default function runFeature(feature: string, gherkinQuery: GherkinQuery, supportCode?: SupportCode): Promise<messages.IEnvelope[]>;
