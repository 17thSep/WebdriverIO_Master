import { Dictionary } from 'lodash';
import { messages } from '@cucumber/messages';
export default class DataTable {
    private readonly rawTable;
    constructor(sourceTable: messages.PickleStepArgument.IPickleTable | string[][]);
    hashes(): any[];
    raw(): string[][];
    rows(): string[][];
    rowsHash(): Dictionary<any>;
    transpose(): DataTable;
}
